{"ast":null,"code":"import { HttpHeaders } from '@angular/common/http';\nimport { BehaviorSubject, of, throwError } from 'rxjs';\nimport { catchError, tap } from 'rxjs/operators';\nimport { environment } from '../../environments/environment';\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"@angular/common/http\";\nexport let JobService = /*#__PURE__*/(() => {\n  class JobService {\n    constructor(http) {\n      this.http = http;\n      this.apiUrl = environment.apiUrl + 'api/jobs/'; // BehaviorSubject to hold the list of jobs reactively\n\n      this.jobsSubject = new BehaviorSubject([]);\n      this.jobs$ = this.jobsSubject.asObservable(); // Cache for jobs list and individual job details\n\n      this.jobsCache = [];\n      this.jobDetailsCache = {}; // Flags to manage fetching state\n\n      this.isJobsFetched = false;\n      this.isFetchingJobs = false;\n    }\n    /**\r\n     * Returns HTTP headers for API requests\r\n     */\n\n\n    getAuthHeaders() {\n      return new HttpHeaders({\n        'Content-Type': 'application/json' // Add Authorization header here if needed, e.g.:\n        // 'Authorization': `Bearer ${token}`\n\n      });\n    }\n    /**\r\n     * Fetch jobs from API only once and cache them\r\n     */\n\n\n    fetchJobs() {\n      if (this.isJobsFetched && this.jobsCache.length > 0) {\n        console.log('Returning cached jobs');\n        this.jobsSubject.next(this.jobsCache);\n        return of(this.jobsCache);\n      }\n\n      if (this.isFetchingJobs) {\n        console.log('Jobs fetch in progress, returning observable');\n        return this.jobs$;\n      }\n\n      this.isFetchingJobs = true;\n      console.log('Fetching jobs from API:', this.apiUrl);\n      return this.http.get(this.apiUrl, {\n        headers: this.getAuthHeaders()\n      }).pipe(tap(jobs => {\n        this.jobsCache = jobs;\n        this.isJobsFetched = true;\n        this.isFetchingJobs = false;\n        this.jobsSubject.next(jobs);\n        console.log('Jobs fetched and cached');\n      }), catchError(error => {\n        this.isFetchingJobs = false;\n        this.handleError(error);\n        return throwError(() => error);\n      }));\n    }\n    /**\r\n     * Get jobs as observable from BehaviorSubject (cached or empty)\r\n     */\n\n\n    getJobs() {\n      return this.jobs$;\n    }\n    /**\r\n     * Get cached jobs synchronously\r\n     */\n\n\n    getCachedJobs() {\n      return this.jobsCache;\n    }\n    /**\r\n     * Check if jobs are cached\r\n     */\n\n\n    areJobsCached() {\n      return this.isJobsFetched && this.jobsCache.length > 0;\n    }\n    /**\r\n     * Fetch job details by ID with caching\r\n     */\n\n\n    getJobById(jobId) {\n      if (this.jobDetailsCache[jobId]) {\n        console.log(`Returning cached job details for jobId: ${jobId}`);\n        return of(this.jobDetailsCache[jobId]);\n      }\n\n      const url = `${this.apiUrl}${jobId}/`;\n      console.log('Fetching job details from API:', url);\n      return this.http.get(url, {\n        headers: this.getAuthHeaders()\n      }).pipe(tap(jobDetails => {\n        this.jobDetailsCache[jobId] = jobDetails;\n        console.log(`Job details cached for jobId: ${jobId}`);\n      }), catchError(error => {\n        this.handleError(error);\n        return throwError(() => error);\n      }));\n    }\n    /**\r\n     * Update a specific job in cache (useful for status updates)\r\n     */\n\n\n    updateJobInCache(jobId, updates) {\n      const jobIndex = this.jobsCache.findIndex(job => job.job_id === jobId);\n\n      if (jobIndex !== -1) {\n        this.jobsCache[jobIndex] = Object.assign(Object.assign({}, this.jobsCache[jobIndex]), updates);\n        this.jobsSubject.next(this.jobsCache);\n        console.log(`Job updated in cache for jobId: ${jobId}`);\n      }\n\n      if (this.jobDetailsCache[jobId]) {\n        this.jobDetailsCache[jobId] = Object.assign(Object.assign({}, this.jobDetailsCache[jobId]), updates);\n        console.log(`Job details updated in cache for jobId: ${jobId}`);\n      }\n    }\n    /**\r\n     * Remove job from cache\r\n     */\n\n\n    removeJobFromCache(jobId) {\n      this.jobsCache = this.jobsCache.filter(job => job.job_id !== jobId);\n      delete this.jobDetailsCache[jobId];\n      this.jobsSubject.next(this.jobsCache);\n      console.log(`Job removed from cache for jobId: ${jobId}`);\n    }\n    /**\r\n     * Clear all caches (jobs list and job details)\r\n     */\n\n\n    clearCache() {\n      this.jobsCache = [];\n      this.jobDetailsCache = {};\n      this.isJobsFetched = false;\n      this.isFetchingJobs = false;\n      this.jobsSubject.next([]);\n      console.log('All caches cleared');\n    }\n    /**\r\n     * Handle HTTP errors with meaningful messages\r\n     */\n\n\n    handleError(error) {\n      let errorMessage = 'An unknown error occurred';\n\n      if (error.status === 401) {\n        errorMessage = 'Unauthorized: Please log in or provide a valid token';\n      } else if (error.status === 404) {\n        errorMessage = `Job not found: ${error.url}. Check if job_id exists in the database.`;\n      } else if (error.status === 0) {\n        errorMessage = `Network error: Unable to reach server at ${error.url}. Ensure backend is running.`;\n      } else {\n        errorMessage = `Error ${error.status}: ${error.message}`;\n      }\n\n      console.error('API error:', errorMessage, error);\n    }\n\n  }\n\n  JobService.ɵfac = function JobService_Factory(t) {\n    return new (t || JobService)(i0.ɵɵinject(i1.HttpClient));\n  };\n\n  JobService.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n    token: JobService,\n    factory: JobService.ɵfac,\n    providedIn: 'root'\n  });\n  return JobService;\n})();","map":null,"metadata":{},"sourceType":"module"}