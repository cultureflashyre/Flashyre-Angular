{"ast":null,"code":"import { HttpHeaders } from '@angular/common/http';\nimport { throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\nimport { environment } from '../../environments/environment'; // Adjust path if needed\n\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"@angular/common/http\";\nexport let McqAssessmentService = /*#__PURE__*/(() => {\n  class McqAssessmentService {\n    constructor(http) {\n      this.http = http;\n      this.apiUrl = environment.apiUrl; // e.g., 'http://localhost:8000/api'\n    }\n    /**\r\n     * Fetches MCQs for a specific job post, grouped by skill.\r\n     * @param jobUniqueId The UUID of the JobPost.\r\n     * @param token JWT token for authentication.\r\n     */\n\n\n    getMcqsForJobPost(jobUniqueId, token) {\n      const headers = new HttpHeaders({\n        Authorization: `Bearer ${token}`\n      }); // **TARGETED URL CHANGE IS HERE**\n      // Determine the base URL (domain + port) by potentially stripping '/api' or '/api/' from this.apiUrl\n\n      let baseUrl = this.apiUrl;\n\n      if (this.apiUrl.endsWith('/api')) {\n        baseUrl = this.apiUrl.substring(0, this.apiUrl.length - '/api'.length);\n      } else if (this.apiUrl.endsWith('/api/')) {\n        baseUrl = this.apiUrl.substring(0, this.apiUrl.length - '/api/'.length);\n      } // Now baseUrl should be like 'http://localhost:8000'\n      // Construct the endpoint for job post MCQs, which is NOT under '/api/' according to project urls.py\n\n\n      const endpointUrl = `${baseUrl}/job-post/${jobUniqueId}/mcqs/`;\n      return this.http.get(endpointUrl, {\n        headers\n      }).pipe(map(response => {\n        if (response.status === 'success' && response.data) {\n          return response.data;\n        } // If status is not 'success' or data is missing, treat as an error.\n\n\n        console.error('Failed to fetch MCQs, unexpected response structure:', response);\n        throw new Error('Failed to fetch MCQs or unexpected response structure from server.');\n      }), catchError(this.handleError) // Centralized error handling\n      );\n    }\n    /**\r\n     * Saves the assessment details.\r\n     * @param payload The assessment data.\r\n     * @param token JWT token for authentication.\r\n     */\n\n\n    saveAssessment(payload, token) {\n      const headers = new HttpHeaders({\n        Authorization: `Bearer ${token}`,\n        'Content-Type': 'application/json'\n      }); // This endpoint IS under /api/ as per your project urls.py: path('api/mcq-assessments/', ...)\n      // And mcq_assessment/urls.py has: path('create/', ...)\n      // So, this.apiUrl (e.g. http://localhost:8000/api) + /mcq-assessments/create/ is correct.\n\n      const endpoint = `${this.apiUrl}/mcq-assessments/create/`;\n      return this.http.post(endpoint, payload, {\n        headers\n      }).pipe(map(response => {\n        if (response.status === 'success' && response.data) {\n          return response.data;\n        }\n\n        if (response.errors) {\n          const allErrorValues = Object.values(response.errors); // Flatten in case error values are arrays (common in DRF for field errors)\n\n          const flattenedErrors = allErrorValues.reduce((acc, val) => {\n            if (Array.isArray(val)) {\n              return acc.concat(val.map(String));\n            }\n\n            return acc.concat(String(val));\n          }, []);\n          const errorMessages = flattenedErrors.join(' ');\n          throw new Error(errorMessages || 'Assessment saving failed due to validation errors.');\n        }\n\n        console.error('Failed to save assessment, unexpected response structure:', response);\n        throw new Error('Failed to save assessment or unexpected response structure from server.');\n      }), catchError(this.handleError) // Centralized error handling\n      );\n    }\n\n    handleError(error) {\n      let errorMessage = 'An unknown error occurred!';\n\n      if (error.error instanceof ErrorEvent) {\n        // Client-side or network error.\n        errorMessage = `A client-side error occurred: ${error.error.message}`;\n      } else {\n        // The backend returned an unsuccessful response code.\n        // The response body may contain clues as to what went wrong.\n        console.error(`Backend returned code ${error.status}, ` + `body was:`, error.error); // Log the full error response from backend\n\n        if (error.status === 0) {\n          errorMessage = 'Cannot connect to the server. Please check your network connection or if the server is running.';\n        } else if (error.error && typeof error.error === 'string') {\n          errorMessage = error.error; // Plain text error\n        } else if (error.error && error.error.message && typeof error.error.message === 'string') {\n          errorMessage = error.error.message; // Error structure like { message: \"...\" }\n        } else if (error.error && error.error.detail && typeof error.error.detail === 'string') {\n          errorMessage = error.error.detail; // DRF default for some errors\n        } else if (error.error && error.error.errors && typeof error.error.errors === 'object') {\n          // DRF validation errors (e.g., from serializers)\n          const errors = error.error.errors;\n          const messages = Object.keys(errors).map(key => {\n            const errorValue = errors[key]; // errorValue is typically an array of strings for field errors\n\n            const errorText = Array.isArray(errorValue) ? errorValue.join(', ') : String(errorValue);\n            return `${key}: ${errorText}`;\n          }).join('; ');\n          errorMessage = messages || `Validation errors occurred (Status: ${error.status})`;\n        } else {\n          errorMessage = `Server error (Status: ${error.status}). Please try again later.`;\n        }\n      } // It's good practice to return a user-friendly error message.\n\n\n      return throwError(() => new Error(errorMessage));\n    }\n\n  }\n\n  McqAssessmentService.ɵfac = function McqAssessmentService_Factory(t) {\n    return new (t || McqAssessmentService)(i0.ɵɵinject(i1.HttpClient));\n  };\n\n  McqAssessmentService.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n    token: McqAssessmentService,\n    factory: McqAssessmentService.ɵfac,\n    providedIn: 'root'\n  });\n  return McqAssessmentService;\n})();","map":null,"metadata":{},"sourceType":"module"}